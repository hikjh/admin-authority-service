project(':application') {

    bootJar { enabled = false }
    jar { enabled = true }

    dependencies {
        implementation group: 'org.springframework', name: 'spring-tx', version: '6.1.5'

        implementation project(':common')
        implementation project(':domain')
    }
}